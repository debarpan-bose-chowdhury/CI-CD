name: Example Python Consumer Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read
  deployments: write
  security-events: write
  packages: write

jobs:
  ci:
    name: CI (Python)
    uses: DEBARPAN2000/CI-CD/.github/workflows/ci.yml@main
    with:
      language: python
      python-version: '3.12'
      python-workdir: .
      python-install-command: 'pip install -r requirements-dev.txt'
      python-test-command: 'pytest --cov=src'
      lint-command: 'pylint src'
      run-sonarqube: true
      sonar-project-key: my-org/my-python-app
    secrets:
      sonar-host-url: ${{ secrets.SONAR_HOST_URL }}
      sonar-token: ${{ secrets.SONAR_TOKEN }}

  build-docker:
    name: Build Docker Image
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    uses: DEBARPAN2000/CI-CD/.github/workflows/docker-build-push.yml@main
    with:
      language: python
      registry: ghcr.io
      image-name: my-python-app
      image-tag: ${{ github.sha }}
      build-context: .
      python-version: '3.12'
    secrets:
      REGISTRY_USERNAME: ${{ github.actor }}
      REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}

  deploy-staging:
    name: Deploy to Staging
    needs: build-docker
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    uses: DEBARPAN2000/CI-CD/.github/workflows/deploy-staging.yml@main
    with:
      image-ref: ${{ needs.build-docker.outputs.image-ref }}
      environment-name: staging
      compose-file: docker-compose.staging.yml
      compose-service: python-app
      health-check-url: http://staging-app:5000/health
      health-check-max-retries: '30'
      health-check-delay: '10'

  dast-scan:
    name: DAST Security Scan
    needs: deploy-staging
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    uses: DEBARPAN2000/CI-CD/.github/workflows/dast-smoke.yml@main
    with:
      target-url: http://staging-app:5000
      dast-threshold: medium
      smoke-endpoints: /health,/api/status

  approval:
    name: Manual Approval for Production
    needs: dast-scan
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment: production-approval
    steps:
      - run: echo "✓ Approved for production"

  deploy-prod:
    name: Deploy to Production
    needs: [build-docker, approval]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    uses: DEBARPAN2000/CI-CD/.github/workflows/deploy-production.yml@main
    with:
      image-ref: ${{ needs.build-docker.outputs.image-ref }}
      environment-name: production
      compose-file: docker-compose.yml
      compose-service: python-app
      health-check-url: http://app:5000/health

  monitor:
    name: Continuous Monitoring
    needs: deploy-prod
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    uses: DEBARPAN2000/CI-CD/.github/workflows/continuous-monitoring.yml@main
    with:
      image-ref: ${{ needs.build-docker.outputs.image-ref }}
      monitoring-url: http://app:5000/health
